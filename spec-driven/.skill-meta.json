{
  "name": "spec-driven",
  "description": "Specify what to build — scope detection, requirements, acceptance criteria.",
  "triggers": ["/spec", "/spec quick", "/spec medium", "/spec large", "/spec resume"],
  "scope": "public",
  "audience": "development-teams",
  "version": "2.0.0",
  "dependencies": {
    "required": [],
    "optional": []
  }
}
